let obj ={
    item:"pen",
    price: 10,
};


console.log(`the cost of ${obj.item} is ${obj.price} rupees`);
 
// Template literals

let speacialString = `This is a template literal`;
console.log(typeof speacialString);


let number = `The number is ${10 + 5}`;
console.log(number);